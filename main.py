from app.app_setup import app
import src.users.users_api

"""
How this works:
- the base app is imported from app_setup
- then the user_api (or other) is loaded, which will add the apis
"""




